import type { SchemaTypeDefinition } from 'sanity'
import ctaBanner from './ctaBanner'
import home from './home'
import programa from './programa'

export const schemaTypes: SchemaTypeDefinition[] = [home, programa, ctaBanner]
